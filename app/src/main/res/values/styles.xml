<resources>

    <!-- Base application theme. -->
    <style name="CommonTheme_orange" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/actionBar</item>
        <item name="colorPrimaryDark">@color/notificationBar</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="PlayerActivityTheme_orange" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/actionBar</item>
        <item name="colorPrimaryDark">@android:color/black</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

</resources>
